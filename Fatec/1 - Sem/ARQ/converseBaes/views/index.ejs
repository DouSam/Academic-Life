<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, height=device-height , initial-scale=1, shrink-to-fit=no">
    <title>Conversor de bases</title>
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@500&display=swap" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href='/stylesheets/main.css'>
    <link rel="icon" href="twotone_calculate_black_24dp.png">
</head>
<body>
    <div class="form-signin">
        <h1>Conversor de Bases</h1>
        <hr>
        <div id="opcao">
            <form action="/" method="get">
                <label for="base1">Insira a primeira base:</label>
                <select name="base1" id="base1">
                    <% for(let i=2; i<36;i++){ %>
                    <option value="<%= i%>"><%= i %></option>
                    <% } %>
                </select>
                <label for="base2">Insira a segunda base:</label>
                <select name="base2" id="base1">
                    <% for(let i=2; i<36;i++){ %>
                    <option value="<%= i%>"><%= i %></option>
                    <% } %>
                </select>
                <label for="numero">Insira o numero a ser convertido</label>
                <input type="text" name="numero" id="numero">
                <div id="botao">
                    <button type="submit">Converter</button>
                </div>
            </form>
        </div>
        <hr>
        <% if(numero != 0){ %>
            <p>
                Número convertido é igual a: <%= numero %>
            </p>
        <% } %>
    </div>
    <div class="footer">
        ©Douglas - 2020
    </div>
</body>
   
</html>